import type { FC } from "react";
import { Typography } from "@mui/material";



const NotFound: FC = () => (
    <Typography sx={{ fontSize: "3rem", textAlign: "center" }}>404 Error - Not found</Typography>
);

export default NotFound;